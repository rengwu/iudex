# Issue Tracker Configuration

Issues live as local markdown files under `.scratch/<feature-slug>/`.

- PRD → `.scratch/<feature-slug>/PRD.md`
- Triage state → `Status:` line near the top of each file
